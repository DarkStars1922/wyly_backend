# 🎵 音乐生成器配置说明

## 翻译功能控制

由于在线翻译服务可能较慢或不稳定，翻译功能**默认已禁用**。

### 启用翻译功能

如果您需要启用中文翻译功能，请设置环境变量：

```bash
# 方法1: 临时启用（当前会话）
export ENABLE_TRANSLATION=true
python manage.py runserver

# 方法2: 在 .env 文件中永久启用
echo "ENABLE_TRANSLATION=true" >> .env
```

### 禁用翻译功能（默认）

```bash
# 不设置环境变量，或显式禁用
export ENABLE_TRANSLATION=false
```

**禁用翻译时的行为：**
- ✅ 英文输入：直接使用，正常工作
- ⚠️ 中文输入：会传递给模型，但模型可能无法正确理解

## 使用建议

### 方案1：直接使用英文（推荐）
```
输入: "romantic piano melody with soft strings"
结果: ✅ 快速生成，效果最好
```

### 方案2：启用翻译功能
```bash
export ENABLE_TRANSLATION=true
python manage.py runserver
```
```
输入: "浪漫的钢琴旋律"
流程: 中文 → 翻译(3秒超时) → 英文 → 生成
结果: ✅ 支持中文，但可能较慢
```

### 方案3：使用在线翻译工具
```
1. 在翻译网站输入中文
2. 获取英文翻译
3. 直接使用英文描述
结果: ✅ 最可控，翻译质量最好
```

## 进度显示优化

已优化进度显示系统：

- ✅ **基于真实时间估算**：根据音频时长（×1.8）预估生成时间
- ✅ **更频繁更新**：每0.5秒更新一次，更流畅
- ✅ **非线性进度**：开始快，接近完成时慢，符合实际体验
- ✅ **详细阶段提示**：清晰显示当前处理步骤

### 预估时间参考

| 音频时长 | 预计生成时间 | 实际可能时间 |
|---------|-------------|-------------|
| 5秒     | ~9秒        | 5-15秒      |
| 10秒    | ~18秒       | 10-25秒     |
| 15秒    | ~27秒       | 15-35秒     |
| 30秒    | ~54秒       | 30-70秒     |

## 性能优化建议

1. **使用较短时长**：推荐5-15秒，生成更快
2. **使用英文描述**：避免翻译延迟
3. **清晰的描述**：具体的乐器、风格、节奏描述效果更好

## 问题排查

### 问题：进度条卡住不动
**可能原因：**
- 模型正在加载（首次使用需要下载模型）
- GPU/CPU资源不足

**解决方案：**
- 查看后端控制台日志
- 等待模型加载完成（约1分钟）

### 问题：翻译太慢
**解决方案：**
- 禁用翻译功能（默认）
- 直接使用英文描述

### 问题：生成失败
**可能原因：**
- 描述不清晰或包含模型不理解的内容
- 内存不足

**解决方案：**
- 使用简单清晰的英文描述
- 减少生成时长
- 查看后端日志了解详细错误

## 启动服务器

```bash
cd /home/ubuntu/health/music_h
source venv/bin/activate

# 禁用翻译（默认，快速）
python manage.py runserver 0.0.0.0:8000

# 或启用翻译
export ENABLE_TRANSLATION=true
python manage.py runserver 0.0.0.0:8000
```

## 总结

✅ **翻译功能已优化**：默认禁用，避免延迟
✅ **进度显示已优化**：基于真实时间，更流畅
✅ **推荐使用英文**：最快速稳定的方案
